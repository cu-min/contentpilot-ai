import { Card, Typography } from 'antd';

export default function AiGenerate() {
  return (
    <Card className="page-panel">
      <Typography.Title level={3}>AI文章生成</Typography.Title>
      <Typography.Text className="muted-text">阶段 3 后续接入文章生成能力。</Typography.Text>
    </Card>
  );
}
