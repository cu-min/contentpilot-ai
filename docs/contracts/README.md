# 契约说明

这里保存跨前端、后端或外部平台长期有效的契约。

简单任务可以在对应任务文档中直接描述接口和状态，不要求为了形式创建完整规范。满足以下情况时再增加独立契约文件：

- 多个页面或模块复用同一接口
- 前后端会并行实现
- 涉及公共状态机或错误码
- 外部平台行为需要可重复的测试替身
- 契约预计会被后续任务继续使用

契约由架构 Agent 维护。冻结后，其他 Agent 不得静默改变。
